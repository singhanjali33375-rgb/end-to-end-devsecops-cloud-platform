groups:
- name: kubernetes-alerts
  rules:
  - alert: PodDown
    expr: kube_pod_status_phase{phase="Failed"} > 0
    for: 1m
    labels:
      severity: critical
    annotations:
      description: Pod has crashed
